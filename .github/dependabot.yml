version: 2
updates:
  # Security updates only for npm.
  # open-pull-requests-limit: 0 disables routine version bumps, but Dependabot
  # still opens PRs for known vulnerabilities (grouped below).
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 0
    versioning-strategy: increase
    groups:
      security-patches:
        applies-to: security-updates
        patterns:
          - '*'

  # Actions: keep a small weekly batch, no major churn spam.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 2
    groups:
      github-actions:
        patterns:
          - '*'
